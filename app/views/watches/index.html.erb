<section class="watches">
	<% @watches.each do |watch| %>
	
		<div class="watch">
			<!-- Add in link to show page -->
			<%= link_to watch_path(watch) do %>
				<!-- add in image here -->
				<div class="price">
					<!-- link this to new orders -->
					<span><%= watch.price_in_pence %></span>
				</div>
				<h2><%= watch[:name] %></h2>
			<% end %>
		</div>
		
	
	<% end %>
</section>
